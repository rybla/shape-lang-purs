{
  "private": true,
  "devDependencies": {
    "parcel": "^2.3.2",
    "purescript": "0.14.5",
    "spago": "^0.20.7"
  },
  "scripts": {
    "test": "spago test",
    "build": "spago bundle-app --main Main --to dist/app.js",
    "serve": "cd dist/; parcel index.html"
  }
}
