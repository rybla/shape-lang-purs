{
  "private": true,
  "devDependencies": {
    "parcel": "^2.3.2",
    "purescript": "0.14.5",
    "spago": "^0.20.7",
    "uuid": "^8.3.2"
  },
  "scripts": {
    "test": "spago test",
    "build": "spago build --purs-args \"--censor-warnings -gsourcemaps,js\"",
    "bundle": "spago bundle-app --purs-args \"--censor-warnings\" --main Main --source-maps --to dist/app.js",
    "serve": "cd dist/; parcel index.html",
    "run": "spago run"
  },
  "dependencies": {
    "purescript-psa": "^0.8.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
